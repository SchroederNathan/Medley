name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  update:
    name: EAS Update → production
    type: update
    environment: production
    params:
      channel: production
